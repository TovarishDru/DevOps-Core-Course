---
# Deployment playbook - application deployment only
# Deploys the containerized application

- name: Deploy application
  hosts: webservers
  become: yes
  vars_files:
    - ../group_vars/all.yml

  roles:
    - app_deploy