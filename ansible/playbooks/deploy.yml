---
# Deployment playbook - application deployment only
# Deploys the containerized application using Docker Compose
# Uses web_app role with Docker Compose

- name: Deploy application
  hosts: webservers
  become: yes
  vars_files:
    - ../group_vars/all.yml

  roles:
    - web_app